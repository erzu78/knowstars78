server:
  port: 8000
spring:
  application:
    name: likecomment-consumer
  redis:
    host: localhost
    port: 6379
    password: 123
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
feign:
  hystrix:
    enabled: false